<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cal.mapper.MemberMapper">

  <insert id="insertMember" parameterType="com.cal.dto.MemberDto">
    INSERT INTO member (username, password, name, email)
    VALUES (#{username}, #{password}, #{name}, #{email})
  </insert>

  <select id="findByUsername" parameterType="String" resultType="com.cal.dto.MemberDto">
    SELECT * FROM member WHERE username = #{username}
  </select>

</mapper>
